// Placeholder admin Firebase module for Phase 1 scaffold.
// Phase 4 will replace this with Firebase Admin SDK initialization.

export const firebaseAdmin = {
  app: null,
  auth: null,
  db: null,
  storage: null,
}
