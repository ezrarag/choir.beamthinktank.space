// Placeholder client Firebase module for Phase 1 scaffold.
// Phase 4 will replace this with initialized Firebase client SDK services.

export const firebaseClient = {
  app: null,
  auth: null,
  db: null,
  storage: null,
}
